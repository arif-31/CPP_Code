/*
 * Code from
 * Ali Ashadullah Arif
 * IT-18031_*_2017-18
 * Dept.Of.ICT,MBSTU
 * Date:2020-04-28-15.04
 * UVA - 821
 */
#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
int adj[105][105];
bool r[105];
int main()
{
	int a,b,count=1;
	for(int i=0; i<105; i++) {
		for

	}
while(1) {
		memset(adj,100,sizeof(adj));
		memset(r,false,sizeof(r));
		
		while(cin>>a>>b and a,b){
		   r[a]=true;
		   r[b]=true;
		   adj[a][b]=1;
		   
		}
		for(int k=0;k<101;k++)
		for(int i=0;i<101;i++)
		for(int j=0;j<101;j++){
		   adj[i][j]=min(adj[i][j],)
		}
	}
}
